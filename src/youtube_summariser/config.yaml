# LLM Configuration
# Supported providers: "openai" or "anthropic" or "google"
provider: "google"

# Model settings per provider
openai:
  model: "gpt-5.2"
  max_tokens: 3000

anthropic:
  model: "claude-sonnet-4-5-20250929"
  max_tokens: 3000

google:
  model: "gemini-3-flash-preview"
  max_tokens: 8000
